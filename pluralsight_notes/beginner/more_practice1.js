"use strict";

const taxRate = ;
const phonePrice = ;
const accessoryPrice = ;
const spendingThreshold = ;

var bankAccountBalance = ;

function taxCalculator(phonePrice, taxRate) {
  var tax = phonePrice * taxRate;
  return tax;
}

for (bankAccountBalance > spendingThreshold)
function formatting() {
  console.log("The total amount to pay is: $" + totalAmount.toFixed(2));
}
